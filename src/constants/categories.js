const meeting = { id: 1, slug: 'meeting' }
const saw = { id: 2, slug: 'saw' }
const read = { id: 3, slug: 'read' }
const outside = { id: 4, slug: 'outside' }
const inside = { id: 5, slug: 'inside' }

const categories = [meeting, saw, read, outside, inside]

export default categories
